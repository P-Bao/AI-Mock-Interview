services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: evaluation-module-backend
    env_file:
      - backend/.env
    ports:
      - "8001:8001"
    depends_on:
      - redis
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "python -c \"import json, urllib.request; data = json.loads(urllib.request.urlopen('http://127.0.0.1:8001/ready').read()); raise SystemExit(0 if data.get('ready') else 1)\"",
        ]
      interval: 30s
      timeout: 10s
      retries: 20
      start_period: 2m

  redis:
    image: redis:8.6.2-alpine
    container_name: evaluation-module-redis
    ports:
      - "6380:6379"

